# Base URL of the Lendwise API. Defaults to https://lendwise.fi.
# Point at http://localhost:3000 to develop against a local web checkout.
LENDWISE_API_URL=https://lendwise.fi

# Set to 1 to run the network integration test (hits the live API).
LENDWISE_INTEGRATION=0
